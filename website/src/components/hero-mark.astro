<div class="mark-scene pointer-events-none absolute inset-0 overflow-hidden select-none" aria-hidden="true">
    <canvas class="mark-canvas"></canvas>
</div>

<script>
    import { mountHeroMark } from '../scripts/hero-mark';

    const canvas = document.querySelector<HTMLCanvasElement>('.mark-canvas');
    if (canvas) mountHeroMark(canvas);
</script>

<style>
    .mark-scene {
        z-index: 0;
    }

    .mark-canvas {
        position: absolute;
        top: 50%;
        left: 40%;
        width: min(54rem, 125%);
        aspect-ratio: 1;
        translate: -50% -50%;
        opacity: 0;
        mask-image: radial-gradient(circle at 50% 50%, #000 44%, transparent 72%);
    }

    @media (width < 64rem) {
        .mark-canvas {
            left: 50%;
            width: min(19rem, 62%);
        }
    }
</style>
