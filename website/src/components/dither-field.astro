<div class="dither-field pointer-events-none absolute inset-0 overflow-hidden select-none" aria-hidden="true">
    <canvas class="dither-canvas"></canvas>
</div>

<script>
    import { mountDitherField } from '../scripts/dither-field';

    const canvas = document.querySelector<HTMLCanvasElement>('.dither-canvas');
    if (canvas) mountDitherField(canvas);
</script>

<style>
    .dither-field {
        z-index: 0;
    }

    .dither-canvas {
        width: 100%;
        height: 100%;
        opacity: 0.6;
        mask-image:
            radial-gradient(44% 64% at 71% 52%, transparent 34%, #000 100%),
            linear-gradient(to bottom, #000 78%, transparent 100%);
        mask-composite: intersect;
        animation: field-in 1.6s ease-out backwards;
    }

    @media (width < 64rem) {
        .dither-canvas {
            opacity: 0.55;
            mask-image: linear-gradient(to bottom, transparent 80%, #000 94%, transparent 100%);
        }
    }

    @keyframes field-in {
        from {
            opacity: 0;
        }
    }

    @media (prefers-reduced-motion: reduce) {
        .dither-canvas {
            animation: none;
        }
    }
</style>
